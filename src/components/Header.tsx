import React from "react";
import logo from "../assets/cortal_logo.png";
const Header = () => {
  return (
    <nav className="header">
      <img src={logo} alt="logo" />
    </nav>
  );
};

export default Header;
